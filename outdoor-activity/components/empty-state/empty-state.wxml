<view class="empty-state">
  <text class="empty-icon">{{icon || '📭'}}</text>
  <text class="empty-text">{{text || '暂无数据'}}</text>
  <view class="empty-action" wx:if="{{actionText}}" bind:tap="onAction">
    <text class="action-text">{{actionText}}</text>
  </view>
</view>
